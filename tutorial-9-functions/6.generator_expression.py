# -----------generator expression
from sys import getsizeof

# ----------list comprehension
# numbers = [ x * 2 for  x in range (15)]
# print(numbers)

# ---generator object

# numbers = (x * 2 for x in range(15))
# print(numbers)
# for y in numbers:
#     print(y)

# -------geeting size of a generator object
